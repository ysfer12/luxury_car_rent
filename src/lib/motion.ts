'use client';

// Export specific components instead of using the default import
export { 
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform,
  animate,
  stagger
} from 'framer-motion';

// Add any other specific exports you need from framer-motion